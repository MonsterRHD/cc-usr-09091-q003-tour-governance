[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "09091-q003"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.116,<1", "asyncpg>=0.30,<1"
]

[tool.pytest.ini_options]
pythonpath = ["."]
